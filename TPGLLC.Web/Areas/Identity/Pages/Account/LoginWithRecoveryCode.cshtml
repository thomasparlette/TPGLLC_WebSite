@page
@model TPGLLC.Web.Areas.Identity.Pages.Account.LoginWithRecoveryCodeModel
@{
    ViewData["Title"] = "Recovery code sign in";
}

<div class="auth-card">
    <h1>Recovery code sign in</h1>

    @if (!string.IsNullOrWhiteSpace(Model.ErrorMessage))
    {
        <div class="auth-alert">@Model.ErrorMessage</div>
    }

    <form method="post" class="auth-form">
        <input type="hidden" asp-for="Input.ReturnUrl" />
        <div class="form-field">
            <label asp-for="Input.RecoveryCode"></label>
            <input asp-for="Input.RecoveryCode" />
            <span asp-validation-for="Input.RecoveryCode"></span>
        </div>
        <button type="submit" class="primary-button">Sign in</button>
    </form>
</div>
