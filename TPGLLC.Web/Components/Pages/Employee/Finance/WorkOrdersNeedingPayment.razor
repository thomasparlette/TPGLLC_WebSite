@page "/portal/employee/finance/work-orders-needing-payment"
@layout CustomerLayout
@attribute [Authorize(Policy = PortalPolicies.Finance)]

<PageTitle>Work Order Needing Payment</PageTitle>

<section class="portal-dashboard">
    <div class="portal-header">
        <p class="section-kicker">Finance Portal</p>
        <h1>Work Order Needing Payment</h1>
        <p>Invoices that still need to be collected.</p>
    </div>

    <div class="portal-section">
        <div class="portal-empty-state">
            <div class="portal-empty-icon">💳</div>
            <div>
                <div class="portal-empty-title">Work Order Needing Payment coming next</div>
                <div class="portal-empty-text">This page is ready for the billing workflow.</div>
            </div>
        </div>
    </div>
</section>
