@using Microsoft.AspNetCore.Components.Authorization
@using TPGLLC.Web.Authorization

<AuthorizeView Context="authState">
    <Authorized>
        <NavLink href="@authState.User.GetPortalHomeHref()" Match="NavLinkMatch.All">
            @authState.User.GetPortalSubtitle()
        </NavLink>

        <a href="/logout">Sign Out</a>
    </Authorized>
    <NotAuthorized>
        <NavLink href="/">Home</NavLink>
        <NavLink href="/services">Services</NavLink>
        <NavLink href="/contact">Contact</NavLink>
        <NavLink href="/Identity/Account/Register">Register</NavLink>
        <NavLink href="/Identity/Account/Login">Login</NavLink>
    </NotAuthorized>
</AuthorizeView>
